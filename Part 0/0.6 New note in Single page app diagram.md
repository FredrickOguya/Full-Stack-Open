sequenceDiagram
  participant browser
  participant server

  Note right of browser: User writes a new note and clicks "Save"

  Note right of browser: JavaScript captures the input<br> and prevents default form submission

  browser->> browser: Create new note object {content, date}
  browser->> browser: Add the new note to the existing list of notes
  browser->> browser: Re-render notes list dynamically on the page

  Note right of browser: The new note apperars instantly<br> without reloading the page

  browser->>server: POST https://studies.cs.helsinki.fi/exampleapp.new_note_spa<br>Body: JSON {content, data }
  activate server
  server-->>browser: 201 Created (success)
  deactivate server

  Note right of browser: Browser receives responsee<br>and confirms data was saved successfully